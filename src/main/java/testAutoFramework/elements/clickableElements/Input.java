package testAutoFramework.elements.clickableElements;

import org.openqa.selenium.By;
import testAutoFramework.elements.BaseElement;

public class Input extends BaseElement {

    public Input(By locator, String name) {
        super(locator, name);
    }

    

}
